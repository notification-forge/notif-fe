mutation CreateTemplate(
  $name: String!
  $type: MessageType!
  $appCode: String!
) {
  createTemplate(input: { name: $name, type: $type, appCode: $appCode }) {
    name
    uuid
    type
    appCode
    createdDate
  }
}
