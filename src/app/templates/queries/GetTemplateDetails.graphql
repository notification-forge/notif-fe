query GetTemplateDetails($id: ID!) {
  template(id: $id) {
    id
    name
    uuid
    type
    appCode
    templateVersions {
      id
      name
      status
      lastModifiedDate
      createdBy
    }
  }
}
