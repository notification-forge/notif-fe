query GetTemplateVersionDetails($templateVersionId: ID!) {
  templateVersion(id: $templateVersionId) {
    templateId
    id
    name
    body
    settings
    version
    status
    createdDate
    lastModifiedDate
  }
}
